task_dataset:

  # PHM_RUL_XJTU:
  #   task_name: RUL_PHM
  #   dataset_name: RUL_XJTU
  #   dataset: RUL_XJTU
  #   data: RUL_XJTU
  #   root_path: /nfsData/nfsShare/WYL/PHM-Large-Model/PHM_dataset/XJTU_RUL_NEW
  #   down_sample: 1
  #   seq_len: 7680
  #   stride_len: 128
  #   enc_in: 2
  #   embed: timeF
  #   num_look_back: 30
  #   num_class: 4
  #   test_idx: [3,6,9]

  PHM_CWRU:
    task_name: classification_PHM
    dataset_name: CWRU
    dataset: CWRU
    data: CWRU
    root_path: /nfsData/nfsShare/WYL/PHM-Large-Model/PHM_dataset/CWRU
    label_type: 'local' # 标签映射方式：默认为local，即本地映射；若为global，则为全局映射
    down_sample: 1 # 降采样系数
    seq_len: 2048 # 输入序列长度
    stride_len: 128 # 滑动窗口步长
    enc_in: 3 # 通道数（不需要传入dataset）
    embed: timeF # 时序特征（为了符合units的data_provider框架）
    num_class: 16 # 分类数
    